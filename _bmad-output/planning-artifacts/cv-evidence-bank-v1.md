# CV evidence bank v1 (pointer)

**Status:** SSOT for **numeric IDs and baselines** lives in **`metrics-manifest.md`** (Deloitte lane v2). This file remains the narrative home for *what* each bullet is allowed to claim; keep both in sync when numbers change.

## How to use

1. Edit **`metrics-manifest.md`** first (add/change `M-*` rows and surfaces).
2. Reflect wording in **`cv/cv-*.html`** and case HTML only from manifest rows.
3. Use **`case-study-stryker-longform.md`** and **`case-study-byjus-scale.md`** for Paige-aligned longform notes before exporting PDFs.

## Birlasoft cluster

Practice pipeline, CXO milestone cadence, template codification — see manifest `M-BIR-*` / `M-STR-*` / `M-HP-*`.

## Consumer cluster

BYJU’s / WHJ — see `M-BYJ-*`, `M-WHJ-01`.

## QA

Before tag: grep each public numeric against its `M-*` row (see Deloitte spec §5).
